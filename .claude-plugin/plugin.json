{
  "name": "dataiku",
  "displayName": "Dataiku Headless",
  "version": "0.2.0",
  "description": "Inspect Dataiku projects through read-only MCP tools and drive Dataiku Cobuild for project-level building — recipes, datasets, ML analyses, dashboards, agents, and more.",
  "author": {
    "name": "Dataiku"
  },
  "homepage": "https://github.com/dataiku/dku-headless",
  "repository": "https://github.com/dataiku/dku-headless",
  "license": "Apache-2.0",
  "keywords": ["dataiku", "dss", "mcp", "cobuild", "agent"],
  "userConfig": {
    "dss_url": {
      "type": "string",
      "title": "DSS URL",
      "description": "Your Dataiku DSS instance URL, for example https://dss.example.com",
      "required": true
    },
    "api_key": {
      "type": "string",
      "title": "DSS personal API key",
      "description": "A personal API key created in Dataiku DSS",
      "sensitive": true,
      "required": true
    }
  },
  "skills": "./dataiku-skills/",
  "mcpServers": {
    "dataiku": {
      "type": "stdio",
      "command": "bash",
      "args": ["${CLAUDE_PLUGIN_ROOT}/bin/run_mcp.sh"],
      "env": {
        "DKU_DSS_URL": "${user_config.dss_url}",
        "DKU_API_KEY": "${user_config.api_key}"
      }
    }
  }
}

__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
.env
.env.*
!.env.example
*.toml.bak
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# Internal development files (not shipped)
.claude/memory/
.claude/plans/
.claude/skills/*
!.claude/skills/skill-optimization/
.claude/scheduled_tasks.lock
# .agents/ is local agent scratch. Exception: the Codex plugin marketplace
# catalog (.agents/plugins/marketplace.json) is force-tracked (git add -f) so
# Codex can discover the dataiku-mcp plugin at its required path.
.agents/

# Brainstorm / design docs
brainstorm/

# The benchmark framework moved to a separate repo; ignore local leftovers.
benchmark/

plans/
plugins/
!dataiku-devkit/skills/**/plugins/
# Temp files
tmp/
demo/
work/
scratch/

# IDE configs
.idea/
plan.md


# AI coding tool configs (benchmark artifacts)
.claude/worktrees/
.omo/

# Benchmark Docker-shared scratch (fixtures + proto dirs)
benchmark/.bench_scratch/

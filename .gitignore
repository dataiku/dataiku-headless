# macOS
.DS_Store

# Python caches
__pycache__/
*.py[cod]

# Virtual environments
.venv/
venv/
.env
.dataiku/config.json

# Claude Code: keep shared settings.json (committed), ignore personal/local bits
.claude/*
!.claude/settings.json
.codex_tmp
tmp

# Codex: MCP server command paths are machine-specific, never commit
.codex/

# Design / rationale docs — local working notes, not tracked
docs/

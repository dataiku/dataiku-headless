{
  "mcpServers": {
    "dataiku": {
      "type": "stdio",
      "command": "bash",
      "args": ["./bin/run_mcp.sh"],
      "env": {
        "DKU_DSS_URL": "${env:DKU_DSS_URL}",
        "DKU_API_KEY": "${env:DKU_API_KEY}"
      }
    }
  }
}

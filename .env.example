# Copy this file to .env and fill in values for local development.

# Environment-configured DSS instance. It takes precedence over the
# `default_instance` selected by config.json.
DKU_INSTANCE_NAME="dev-dss"
DKU_DSS_URL=https://your-instance.dataiku.com
DKU_NO_CHECK_CERTIFICATE=false # true/1/yes skips TLS certificate verification
DKU_API_KEY=your-api-key

# Optional: directory containing config.json and/or .env. When unset, config
# falls back to ./.dataiku/config.json and then the XDG user config directory.
# DKU_CONFIG_DIR=/absolute/path/to/config

# MCP Server Configuration
DKU_MCP_MAX_WORKERS=4
DKU_MCP_TRANSPORT=stdio
DKU_MCP_COBUILD_MODE=CREATE_ONLY
DKU_MCP_TOOL_EXPOSURE=search
DKU_MCP_SEARCH_MAX_RESULTS=5
DKU_MCP_SEARCH_ALWAYS_VISIBLE=get_current_instance

# Long Cobuild turns. A wait timeout returns a pollable turn id; it does not
# cancel the worker. Processes sharing DKU_MCP_STATE_DIR must use the same cap.
DKU_COBUILD_TIMEOUT_SECONDS=1200
DKU_COBUILD_MAX_TIMEOUT_SECONDS=1800
DKU_MCP_MAX_COBUILD_TURNS=8
# DKU_MCP_STATE_DIR=/absolute/path/to/private/state

# Optional streamable-http settings
FASTMCP_HOST=127.0.0.1
FASTMCP_PORT=8000
FASTMCP_STREAMABLE_HTTP_PATH=/mcp
